{
  "name": "loop-launch-orchestrator",
  "version": "1.0.0",
  "description": "Stateless Lambda function URL that orchestrates Remotion Lambda renders for the Loop launch film popup.",
  "type": "module",
  "main": "index.mjs",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.665.0",
    "@remotion/lambda": "4.0.340"
  }
}
